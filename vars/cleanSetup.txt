
===========
Clean setup
===========

 arg: rootName - Root directory containing 'build' and 'install' directories which are to be removed.

Clean function to clean all generated files from a build that uses the setup CMake installation script.  This removes the setup build directory, and the build and install directory trees in the directory passed as an argument.

